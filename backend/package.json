{
  "name": "sav-backend",
  "version": "1.0.0",
  "description": "Unified API — auth, conversations, factures, articles, sav tickets",
  "main": "src/app.js",
  "scripts": {
    "start": "node src/app.js",
    "dev": "nodemon src/app.js",
    "studio": "DATABASE_URL=postgresql://sav_user:sav_password@localhost:5433/sav_db prisma studio",
    "prisma:generate": "prisma generate",
    "postinstall": "prisma generate"
  },
  "dependencies": {
    "@prisma/client": "^6.19.3",
    "axios": "^1.7.2",
    "bcryptjs": "^2.4.3",
    "cors": "^2.8.5",
    "dotenv": "^16.4.5",
    "express": "^4.19.2",
    "express-rate-limit": "^7.3.1",
    "helmet": "^7.1.0",
    "jsonwebtoken": "^9.0.2"
  },
  "devDependencies": {
    "nodemon": "^3.1.4",
    "prisma": "^6.19.3"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
