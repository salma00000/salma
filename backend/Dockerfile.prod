FROM node:18.19.0-alpine3.19

WORKDIR /app

# Install dependencies first (better layer caching)
COPY package*.json ./
RUN npm ci --omit=dev

# Copy application source
COPY . .

EXPOSE 3001

CMD ["node", "src/app.js"]
